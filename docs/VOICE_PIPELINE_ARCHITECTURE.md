# ADRIAN Voice Input Pipeline - Architecture

## System Overview

```
┌─────────────────────────────────────────────────────────────────────┐
│                         USER INTERACTION                            │
│                                                                     │
│                    👤 User speaks to ADRIAN                         │
└──────────────────────────────┬──────────────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────────┐
│                      AUDIO CAPTURE LAYER                            │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │  AudioStream (audio_utils.py)                                 │ │
│  │  - 16kHz, Mono, 30ms chunks (480 samples)                     │ │
│  │  - Continuous real-time capture                               │ │
│  │  - Callbacks: audio_chunk, vad_state, hotword                 │ │
│  └───────────────────────────────────────────────────────────────┘ │
└──────────────┬──────────────────────────┬─────────────────┬─────────┘
               │                          │                 │
               │                          │                 │
┌──────────────▼────────┐    ┌───────────▼────────┐   ┌───▼───────────┐
│   HOTWORD DETECTION   │    │   VAD PROCESSING   │   │  CONVERSATION │
│  (hotword_utils.py)   │    │   (vad_utils.py)   │   │   BUFFERING   │
│                       │    │                    │   │               │
│  Porcupine "ADRIAN"   │    │  WebRTC VAD        │   │  Audio chunks │
│  Sensitivity: 0.7     │    │  State: SPEECH/    │   │  stored for   │
│  Triggers: Start conv │    │  SILENCE/TRANSITION│   │  STT processing│
└──────────────┬────────┘    └───────────┬────────┘   └───┬───────────┘
               │                         │                │
               │                         │                │
               ▼                         ▼                │
┌─────────────────────────────────────────────────────────▼───────────┐
│                   CONVERSATION MANAGER                              │
│               (conversation_manager.py)                             │
│                                                                     │
│  ┌────────────────────────────────────────────────────────────┐   │
│  │ STATE MACHINE:                                             │   │
│  │                                                            │   │
│  │  IDLE ──hotword──► LISTENING ──vad_speech──► PROCESSING   │   │
│  │   ▲                                               │        │   │
│  │   │                                               ▼        │   │
│  │   └──timeout(30s)──────────────────────── CONTINUOUS      │   │
│  │                                               │            │   │
│  │                                               └─repeat─────┘   │
│  └────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  Features:                                                          │
│  - Audio buffering (10s max)                                       │
│  - Context tracking (3 turns)                                      │
│  - Timeout handling                                                │
│  - Performance metrics                                             │
└──────────────────────────────┬──────────────────────────────────────┘
                               │
                               │ Speech ended → trigger STT
                               ▼
┌─────────────────────────────────────────────────────────────────────┐
│                   SPEECH-TO-TEXT PROCESSOR                          │
│                     (stt_utils.py)                                  │
│                                                                     │
│  ┌────────────────────────────────────────────────────────────┐   │
│  │  OpenAI Whisper                                            │   │
│  │  Model: base (75M params, ~150MB)                          │   │
│  │  Device: CPU (or CUDA)                                     │   │
│  │  Features:                                                 │   │
│  │  - Multi-language support                                  │   │
│  │  - High accuracy                                           │   │
│  │  - Offline processing                                      │   │
│  │  - Confidence scoring                                      │   │
│  └────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  ┌────────────────────────────────────────────────────────────┐   │
│  │  Audio Quality Analysis                                    │   │
│  │  - Background noise detection                              │   │
│  │  - Music detection                                         │   │
│  │  - Multiple speakers                                       │   │
│  │  - Clipping/distortion                                     │   │
│  │  - Too quiet/short                                         │   │
│  └────────────────────────────────────────────────────────────┘   │
└──────────────┬────────────────────────────┬─────────────────────────┘
               │                            │
               │ Success                    │ Error
               ▼                            ▼
┌──────────────────────────┐    ┌─────────────────────────────────────┐
│  UTTERANCE PUBLISHING    │    │   PERSONALITY ERROR HANDLER         │
│                          │    │   (personality_error_handler.py)    │
│  ┌────────────────────┐  │    │                                     │
│  │  UtteranceEvent    │  │    │  Error Types:                       │
│  │  {                 │  │    │  - Low confidence                   │
│  │   text: "...",     │  │    │  - Background noise                 │
│  │   confidence: 0.92,│  │    │  - Music detected                   │
│  │   source: "voice", │  │    │  - Multiple speakers                │
│  │   user_id: "...",  │  │    │  - Too short/quiet                  │
│  │   correlation_id   │  │    │  - Processing error                 │
│  │  }                 │  │    │                                     │
│  └────────────────────┘  │    │  Generates witty, helpful responses │
│                          │    │  matching ADRIAN's personality      │
└──────────────┬───────────┘    └─────────────────┬───────────────────┘
               │                                  │
               │                                  │
               ▼                                  ▼
┌─────────────────────────────────────────────────────────────────────┐
│                       REDIS PUB/SUB                                 │
│                                                                     │
│  Channel: "utterances"         Channel: "responses"                │
│  ├─ UtteranceEvent published   ├─ Error response published        │
│  └─ Processing Service picks   └─ Output Service speaks           │
│     up for NLP                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

## Component Communication Flow

```
┌──────────┐
│  User    │
└────┬─────┘
     │ "ADRIAN, open chrome"
     ▼
┌─────────────┐
│ Microphone  │ ──────────► AudioStream
└─────────────┘              │
                             ├──► HotwordDetector ──► "ADRIAN" detected
                             │                         │
                             │                         ▼
                             │                   ConversationManager
                             │                   (starts conversation)
                             │                         │
                             ├──► VAD ─────────────────┤
                             │    (speech detection)   │
                             │                         │
                             └──► Audio Buffer ────────┤
                                  (accumulating)       │
                                                       │
                                         Speech ended  │
                                                       ▼
                                              STTProcessor
                                              (Whisper)
                                                       │
                                         ┌─────────────┴─────────────┐
                                         │                           │
                                    Success                       Error
                                         │                           │
                                         ▼                           ▼
                                 UtteranceEvent         PersonalityErrorHandler
                                         │                           │
                                         ▼                           ▼
                                   Redis Pub/Sub              Error Response
                                  (to Processing)          (to Output Service)
```

## State Transitions

```
                         CONVERSATION STATES

     ┌────────────────────────────────────────────────────┐
     │                                                    │
     │                      IDLE                          │
     │                                                    │
     │  • Listening for hotword                          │
     │  • No active conversation                         │
     │  • Audio stream running                           │
     │                                                    │
     └──────────────┬─────────────────────────────────────┘
                    │
                    │ Hotword "ADRIAN" detected
                    │ on_hotword_detected()
                    ▼
     ┌──────────────────────────────────────────────────────┐
     │                                                      │
     │                   LISTENING                          │
     │                                                      │
     │  • Conversation started                             │
     │  • Waiting for user speech                          │
     │  • Audio buffering active                           │
     │                                                      │
     └──────────────┬───────────────────────────────────────┘
                    │
                    │ VAD: Speech detected
                    │ on_vad_state_change(is_speech=True)
                    │ (still in LISTENING, buffering audio)
                    │
                    │ VAD: Silence detected
                    │ on_vad_state_change(is_speech=False)
                    ▼
     ┌──────────────────────────────────────────────────────┐
     │                                                      │
     │                   PROCESSING                         │
     │                                                      │
     │  • Running Whisper STT                              │
     │  • Analyzing audio quality                          │
     │  • Calculating confidence                           │
     │                                                      │
     └──────────────┬───────────────────────────────────────┘
                    │
                    │ STT complete, valid result
                    │ publish_utterance()
                    ▼
     ┌──────────────────────────────────────────────────────┐
     │                                                      │
     │                   CONTINUOUS                         │
     │                                                      │
     │  • In conversation mode                             │
     │  • No hotword needed for next utterance             │
     │  • Still listening and buffering                    │
     │  • Timeout timer running (30s)                      │
     │                                                      │
     └──────┬───────────────┬───────────────────────────────┘
            │               │
            │               │ User speaks again
            │               │ (no hotword needed)
            │               └──────► Back to speech detection
            │                         (repeat cycle)
            │
            │ 30s timeout reached
            │ should_continue_listening() = False
            └──────────────────────────────────► Back to IDLE
```

## Data Flow

```
┌──────────────┐
│ Audio Input  │ ──► int16, 16kHz, mono, 480 samples/chunk
└──────┬───────┘
       │
       ├──► Hotword Detector ──► boolean (detected/not)
       │    (512 samples)
       │
       ├──► VAD ───────────────► VADState (SPEECH/SILENCE/TRANSITION)
       │    (480 samples)
       │
       └──► Buffer ────────────► accumulated chunks
            (if in conversation)
                                       │
                                       │ Speech ended
                                       ▼
                              ┌────────────────┐
                              │ STT Processor  │
                              └────────┬───────┘
                                       │
                    ┌──────────────────┴──────────────────┐
                    │                                     │
                    ▼                                     ▼
            ┌───────────────┐                    ┌──────────────┐
            │ Transcription │                    │ Audio Quality│
            │ "open chrome" │                    │   Analysis   │
            └───────┬───────┘                    └──────┬───────┘
                    │                                   │
                    ├───────────────┬───────────────────┤
                    │               │                   │
                    ▼               ▼                   ▼
              ┌──────────┐   ┌────────────┐   ┌──────────────┐
              │   Text   │   │ Confidence │   │ Quality Enum │
              │  string  │   │   float    │   │  AudioQuality│
              └────┬─────┘   └─────┬──────┘   └──────┬───────┘
                   │               │                  │
                   └───────────────┼──────────────────┘
                                   │
                                   ▼
                          ┌─────────────────┐
                          │ UtteranceEvent  │
                          │ {               │
                          │   message_id,   │
                          │   correlation,  │
                          │   text,         │
                          │   confidence,   │
                          │   source,       │
                          │   user_id       │
                          │ }               │
                          └────────┬────────┘
                                   │
                                   ▼
                          ┌─────────────────┐
                          │  Redis Pub/Sub  │
                          │ "utterances"    │
                          └─────────────────┘
```

## Configuration Hierarchy

```
                    ┌──────────────┐
                    │   .env file  │
                    │ (user config)│
                    └──────┬───────┘
                           │ overrides
                           ▼
                  ┌──────────────────┐
                  │  shared/config.py│
                  │   (defaults)     │
                  └────────┬─────────┘
                           │ used by
                           ▼
        ┌──────────────────────────────────────┐
        │     services/io_service/main.py      │
        │                                      │
        │  ┌────────────────────────────────┐ │
        │  │ settings = get_settings()      │ │
        │  └────────────────────────────────┘ │
        └──────────┬───────────────────────────┘
                   │ configures
                   ▼
    ┌──────────────────────────────────────────┐
    │         Component Initialization         │
    │                                          │
    │  STTProcessor(model=settings.whisper_*)  │
    │  ConversationManager(timeout=settings.*) │
    │  AudioStream(vad_agg=settings.vad_*)     │
    │  HotwordDetector(sens=settings.hotword*)│
    └──────────────────────────────────────────┘
```

## Error Handling Flow

```
                    ┌────────────────┐
                    │  STT Processing│
                    └────────┬───────┘
                             │
              ┌──────────────┴──────────────┐
              │                             │
          Success                         Error
              │                             │
              ▼                             ▼
    ┌─────────────────┐          ┌──────────────────┐
    │ Confidence Check│          │ Audio Quality    │
    └────────┬────────┘          │ Classification   │
             │                   └────────┬─────────┘
      ┌──────┴──────┐                    │
      │             │                    │
   High          Low                     │
      │             │                    │
      ▼             ▼                    ▼
┌──────────┐  ┌──────────────────────────────────┐
│ Publish  │  │ PersonalityErrorHandler          │
│Utterance │  │                                  │
└──────────┘  │ Error Type:                      │
              │ - LOW_CONFIDENCE                 │
              │ - BACKGROUND_NOISE               │
              │ - MUSIC_DETECTED                 │
              │ - MULTIPLE_SPEAKERS              │
              │ - TOO_SHORT                      │
              │ - NO_SPEECH                      │
              │ - PROCESSING_ERROR               │
              │                                  │
              │ Generate response:               │
              │ "I beg your pardon, Sir..."      │
              └───────────┬──────────────────────┘
                          │
                          ▼
                  ┌────────────────┐
                  │ Publish Error  │
                  │   Response     │
                  │ to Redis       │
                  └────────────────┘
```

## Performance Monitoring

```
                 ┌─────────────────────┐
                 │  REST API Endpoints │
                 └──────────┬──────────┘
                            │
        ┌───────────────────┼───────────────────┐
        │                   │                   │
        ▼                   ▼                   ▼
┌───────────────┐  ┌─────────────────┐  ┌──────────────┐
│ /health       │  │ /status/        │  │ /status/stt  │
│               │  │ conversation    │  │              │
│ Overall       │  │                 │  │ Processing   │
│ system health │  │ State, context, │  │ time, RT     │
│               │  │ turns, stats    │  │ factor       │
└───────────────┘  └─────────────────┘  └──────────────┘

                  ┌─────────────────────┐
                  │   Logging System    │
                  │  (logging_config.py)│
                  └──────────┬──────────┘
                             │
                    ┌────────┴────────┐
                    │                 │
                    ▼                 ▼
              ┌──────────┐      ┌─────────┐
              │ Console  │      │ SQLite  │
              │  Output  │      │   DB    │
              └──────────┘      └─────────┘
                                logs/adrian.db
```

## Memory Management

```
┌─────────────────────────────────────────────────────────────┐
│                    Audio Buffer                             │
│                                                             │
│  Conversation Manager:                                      │
│  ┌────────────────────────────────────────────────────┐   │
│  │ deque(maxlen = 10 seconds * 16000 Hz)             │   │
│  │ = 160,000 samples max                              │   │
│  │ = ~320KB memory (int16)                            │   │
│  └────────────────────────────────────────────────────┘   │
│                                                             │
│  Cleared after:                                             │
│  - Successful STT transcription                             │
│  - Conversation timeout                                     │
│  - Error requiring restart                                  │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                  Context History                            │
│                                                             │
│  Conversation Manager:                                      │
│  ┌────────────────────────────────────────────────────┐   │
│  │ deque(maxlen = 3 turns * 2 roles)                 │   │
│  │ = 6 conversation entries max                       │   │
│  │ Each: ~1KB (text + metadata)                       │   │
│  │ Total: ~6KB                                        │   │
│  └────────────────────────────────────────────────────┘   │
│                                                             │
│  Cleared after:                                             │
│  - Conversation ends                                        │
│  - Timeout reached                                          │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                    Whisper Model                            │
│                                                             │
│  Size by model:                                             │
│  - tiny: ~75MB                                              │
│  - base: ~150MB  ← default                                 │
│  - small: ~500MB                                            │
│  - medium: ~1.5GB                                           │
│  - large: ~3GB                                              │
│                                                             │
│  Loaded once at startup, kept in memory                     │
└─────────────────────────────────────────────────────────────┘
```

---

## Technologies Used

| Component | Technology | Version | Purpose |
|-----------|-----------|---------|---------|
| Audio I/O | sounddevice | latest | Microphone/speaker access |
| Hotword | Porcupine | latest | "ADRIAN" wake word |
| VAD | webrtcvad | latest | Speech detection |
| STT | OpenAI Whisper | 20231117+ | Transcription |
| Pub/Sub | Redis | latest | Event distribution |
| API | FastAPI | latest | REST endpoints |
| Config | Pydantic Settings | latest | Configuration management |
| Logging | Custom SQLite | N/A | Structured logging |

---

**This architecture enables real-time, conversational voice interaction with ADRIAN while maintaining high quality transcription, context awareness, and personality-driven error handling.**

