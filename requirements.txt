# ============================================================================
# A.D.R.I.A.N - Core Dependencies
# Autonomous Dreams Rest In Ambitions Naturally
# ============================================================================

# Web Framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.10.0  # Newer version with pre-built wheels for Python 3.13
pydantic-settings>=2.1.0
python-multipart>=0.0.6

# Redis (Message Bus)
redis>=5.0.1

# PostgreSQL (Database)
asyncpg>=0.29.0
psycopg2-binary>=2.9.9
sqlalchemy[asyncio]>=2.0.25

# Vector Store (FAISS)
faiss-cpu>=1.12.0  # Use faiss-gpu if you have CUDA
numpy>=1.26.0  # Allow newer versions for Python 3.13 compatibility

# HTTP Client
httpx>=0.26.0

# Embeddings & NLP
sentence-transformers>=2.3.1  # For generating embeddings
torch>=2.1.2  # PyTorch for sentence-transformers
transformers>=4.37.2

# Google Gemini API (Cloud LLM)
google-generativeai>=0.3.2

# Audio Processing (Phase 2 - Active)
sounddevice>=0.4.6  # Microphone capture and playback
soundfile>=0.12.1  # Audio file I/O (WAV, FLAC, OGG)
scipy>=1.11.0  # Signal processing for voice effects

# Text-to-Speech (Phase 2 - Active)
TTS>=0.22.0  # Coqui TTS (high quality, local)

# Speech-to-Text (Phase 2 - Active)
openai-whisper>=20231117  # OpenAI Whisper for high-quality STT
# vosk>=0.3.45  # Alternative STT engine (lighter but less accurate)

# Voice Activity Detection (Phase 2 - Active)
webrtcvad>=2.0.10  # WebRTC VAD for speech detection

# Hotword Detection (Phase 2 - Active)
pvporcupine>=3.0.0  # Porcupine for wake word detection

# Cryptography & Security
cryptography>=42.0.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# System Integration
psutil>=5.9.7  # System monitoring and control

# Development Tools
pytest>=7.4.4
pytest-asyncio>=0.23.3
black>=23.12.1
flake8>=7.0.0
mypy>=1.8.0

# Logging & Monitoring
python-json-logger>=2.0.7

# Utilities
python-dotenv>=1.0.0
